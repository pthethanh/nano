// Package server provides a small gRPC server wrapper with optional HTTP gateway
// support, shared listener/address configuration, and helpers for working with
// incoming metadata and interceptor context.
package server
